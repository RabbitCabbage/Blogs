---
import PostMeta from '~/components/PostMeta.astro'
import type { Post } from '~/types'

interface Props {
  post: Post
}

const { post } = Astro.props
---

<article class="prose">
  <PostMeta post={post} />
  <div class="not-prose">
    <slot name="toc" />
  </div>
  <slot />
</article>
